// `import.meta.glob` and `?raw` imports are Vite's, and the demo is the only
// thing here that uses them — the library is built with no ambient types at
// all, which is why this is a reference rather than an entry in `tsconfig.json`.
/// <reference types="vite/client" />
